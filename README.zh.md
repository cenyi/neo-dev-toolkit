# JSON 格式化与多格式转换工具

<div align="center">

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Astro](https://img.shields.io/badge/Astro-4.15-FF5D01.svg)
![Cloudflare](https://img.shields.io/badge/Cloudflare-Pages-orange.svg)

**终极 JSON 工具集 - 20+ 种格式转换与代码生成**

[功能特性](#-核心功能) • [快速开始](#-快速开始) • [文档](#-文档) • [在线演示](https://tojsons.com)

</div>

---

## ✨ 项目概述

一个强大的基于浏览器的 JSON 工具集，支持 20+ 种格式转换、代码生成和高级 JSON 操作。100% 客户端处理 - 您的数据永远不会离开浏览器！

### 🌟 核心亮点

- 🎯 **智能输入检测**：自动识别 JSON、URL参数、XML、YAML 等 15+ 种格式
- 🔄 **20+ 种转换**：JSON ↔ YAML/XML/CSV/HTML/PDF/表格 等
- 💻 **代码生成**：TypeScript、Dart、Go、Rust、Python、C、JSON Schema
- 🔍 **高级工具**：JSON 对比、差异分析、字段提取、验证
- 🌐 **双语支持**：完整的英文和中文界面
- 📱 **移动端优化**：完美适配所有设备
- 🔒 **隐私优先**：100% 浏览器端处理，无服务器上传
- ⚡ **极速性能**：轻松处理 10MB+ 的 JSON 文件

---

## 🚀 快速开始

### 安装

```bash
# 克隆仓库
git clone https://github.com/cenyi/neo-dev-toolkit.git

# 进入项目目录
cd neo-dev-toolkit

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

### 快速演示

1. 打开浏览器访问 `http://localhost:4321`
2. 将任何 JSON 粘贴到编辑器中
3. 观看即时自动格式化和验证
4. 点击任意转换按钮来转换您的数据

---

## 🎯 核心功能

### JSON 核心操作

#### 1. **智能输入检测** 🧠
自动检测并解析：
- ✅ 标准 JSON
- ✅ URL 参数（查询字符串）
- ✅ XML 文档
- ✅ YAML 文件
- ✅ 带注释的 JSON
- ✅ 压缩的 JSON

**示例**：只需粘贴 `?name=张三&age=30` 即可自动转换为 JSON！

#### 2. **JSON 格式化与验证** ✨
- 实时语法高亮
- 带行号的错误检测
- 粘贴时自动格式化
- 可折叠的树形视图
- 行号导航

#### 3. **JSON 对比与差异分析** 🔄
- 并排对比视图
- 可视化差异高亮：
  - 🟢 绿色：新增
  - 🔴 红色：删除
  - 🟡 黄色：修改
- 完美用于 API 测试和配置变更

#### 4. **字段提取** 🔍
使用 JavaScript 表达式提取数据：

```javascript
// 提取嵌套字段
obj => obj.user.name

// 从数组中提取
obj => obj.items[0].id

// 数据转换
obj => obj.users.map(u => u.email)

// 过滤和映射
obj => obj.items.filter(i => i.price > 100).map(i => i.name)
```

---

### 🔄 数据格式转换

#### JSON 转 YAML
将 JSON 转换为带正确缩进的 YAML 格式：
```yaml
name: 张三
email: zhangsan@example.com
age: 30
address:
  street: 朝阳区xxx号
  city: 北京
```

#### JSON 转 XML
生成具有正确结构和转义的 XML。

#### JSON 转 CSV/Excel
- 将 JSON 数组转换为 CSV 格式
- 处理嵌套对象
- Excel 兼容输出
- 一键复制到剪贴板

#### JSON 转 HTML
生成带语法高亮的格式化 HTML：
```html
<!DOCTYPE html>
<html>
<head><title>JSON 数据</title></head>
<body>
<pre class="json-key">"name"</pre>: <pre class="json-string">"张三"</pre>
...
</body>
</html>
```

#### JSON 转 PDF
通过浏览器打印导出 PDF，保留格式。

#### JSON 转表格
将 JSON 数据可视化为美观的 HTML 表格：
- 数组转换为行式表格
- 对象转换为键值对表格
- 基于类型的颜色编码
- 响应式设计

---

### 💻 代码生成

#### TypeScript 接口
```typescript
interface UserData {
  name: string;
  email: string;
  age: number;
  address: {
    street: string;
    city: string;
  };
}
```

#### Dart 类（Flutter）
```dart
class UserData {
  String name;
  String email;
  int age;
  Address address;

  UserData({this.name, this.email, this.age, this.address});

  factory UserData.fromJson(Map<String, dynamic> json) {
    return UserData(
      name: json['name'],
      email: json['email'],
      age: json['age'],
      address: Address.fromJson(json['address']),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'name': name,
      'email': email,
      'age': age,
      'address': address?.toJson(),
    };
  }
}
```

#### Go 结构体
```go
type UserData struct {
    Name    string  `json:"name"`
    Email   string  `json:"email"`
    Age     int     `json:"age"`
    Address Address `json:"address"`
}
```

#### Rust 结构体
```rust
#[derive(Serialize, Deserialize)]
struct UserData {
    name: String,
    email: String,
    age: i64,
    address: Address,
}
```

#### Python 数据类
```python
from dataclasses import dataclass
from typing import Optional

@dataclass
class UserData:
    name: str
    email: str
    age: int
    address: Optional['Address'] = None
```

#### C 结构体
```c
typedef struct {
    char* name;
    char* email;
    int age;
    Address* address;
} UserData;
```

#### JSON Schema
生成用于验证的标准 JSON Schema：
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": {"type": "string"},
    "email": {"type": "string"},
    "age": {"type": "number"}
  },
  "required": ["name", "email", "age"]
}
```

---

### 🛠️ 实用工具

#### JSON 压缩
- 移除所有空白字符
- 压缩为单行
- 完美用于生产环境 API

#### JSON 转义
- 转义特殊字符
- 安全的字符串嵌入
- 一键复制

#### 注释移除
- 移除 JSON5 风格的注释
- 为生产环境清理
- 保持数据完整性

#### 历史记录管理
- 本地存储持久化
- 快速访问最近的 JSON
- 隐私优先（仅本地）

---

## 📖 使用示例

### 示例 1：API 响应处理

```json
{
  "status": "success",
  "data": {
    "users": [
      {"id": 1, "name": "Alice", "email": "alice@example.com"},
      {"id": 2, "name": "Bob", "email": "bob@example.com"}
    ]
  }
}
```

**提取所有邮箱**：
```javascript
obj => obj.data.users.map(u => u.email)
```
结果：`["alice@example.com", "bob@example.com"]`

### 示例 2：配置文件转换

**YAML 输入**：
```yaml
server:
  host: localhost
  port: 8080
  ssl: true
```

**自动检测为 YAML** → **转换为 JSON** → **生成 Go 结构体** → **复制到代码**

### 示例 3：JSON 对比

**之前**：
```json
{"name": "张三", "age": 30, "city": "北京"}
```

**之后**：
```json
{"name": "张三", "age": 31, "city": "上海"}
```

**显示**：
- 🟡 `age`: 30 → 31
- 🟡 `city`: "北京" → "上海"

### 示例 4：URL 参数解析

**输入**：`?search=json&page=1&limit=10`

**自动转换为**：
```json
{
  "search": "json",
  "page": 1,
  "limit": 10
}
```

### 示例 5：数组转表格

**输入**：
```json
[
  {"product": "笔记本电脑", "price": 5999, "stock": 50},
  {"product": "鼠标", "price": 99, "stock": 200}
]
```

**生成表格**：
| product | price | stock |
|---------|-------|-------|
| 笔记本电脑 | 5999 | 50 |
| 鼠标 | 99 | 200 |

### 示例 6：代码生成工作流

1. 粘贴 API 响应 JSON
2. 点击"JSON 转 TypeScript"
3. 获取接口定义
4. 在 TypeScript 项目中使用
5. 节省数小时的手动输入时间！

---

## 🎨 界面特性

### 响应式设计
- 🖥️ **桌面端**：多列布局，并排编辑器
- 📱 **平板端**：优化的双列网格
- 📲 **移动端**：单列，触摸友好按钮

### 按钮组织
按钮按功能分组：
```
[基础操作] | [数据转换] | [代码生成] | [分析工具] | [其他]
```

**基础操作**：折叠、展开、移除注释、压缩、转义
**数据转换**：XML、YAML、CSV、Excel、HTML、PDF
**代码生成**：TypeScript、Dart、C、Go、Rust、Python、Schema
**分析工具**：表格、对比
**其他**：清空、历史、字体大小

### 智能特性
- 🎨 **语法高亮**：Monaco Editor（VS Code 引擎）
- 🌓 **主题支持**：自动深色/浅色模式
- 📏 **字体大小控制**：11px - 16px 可调
- 🔤 **双语界面**：即时英文/中文切换
- ⌨️ **键盘快捷键**：快速操作支持

---

## 🔧 技术细节

### 技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| 框架 | Astro | 4.15+ |
| 编辑器 | Monaco Editor | 最新 |
| 样式 | 原生 CSS | - |
| 语言 | JavaScript | ES6+ |
| 部署 | Cloudflare Pages/Workers | - |

### 架构

```
┌─────────────────────────────────────────┐
│           用户浏览器                    │
│  ┌──────────────────────────────────┐  │
│  │    Astro 前端应用               │  │
│  ├──────────────────────────────────┤  │
│  │  • 智能输入检测                  │  │
│  │  • 格式转换引擎                  │  │
│  │  • 代码生成模块                  │  │
│  │  • Monaco Editor 集成            │  │
│  │  • 历史记录管理（本地）          │  │
│  └──────────────────────────────────┘  │
│                                         │
│  100% 客户端处理                        │
│  ❌ 无服务器上传                        │
│  ✅ 完全隐私保护                        │
└─────────────────────────────────────────┘
```

### 性能优化

- **懒加载**：Monaco Editor 按需加载
- **防抖处理**：智能输入处理
- **虚拟滚动**：处理大型 JSON 文件
- **本地存储**：即时历史记录访问
- **CSS 硬件加速**：流畅动画
- **最小化打包**：优化的依赖项

### 浏览器兼容性

| 浏览器 | 版本 | 状态 |
|--------|------|------|
| Chrome/Edge | 90+ | ✅ 完全支持 |
| Firefox | 88+ | ✅ 完全支持 |
| Safari | 14+ | ✅ 完全支持 |
| Opera | 76+ | ✅ 完全支持 |
| Mobile Safari | 14+ | ✅ 完全支持 |
| Chrome Mobile | 90+ | ✅ 完全支持 |

---

## 📦 项目结构

```
neo-dev-toolkit/
├── src/
│   ├── pages/
│   │   ├── index.astro          # 英文首页
│   │   └── zh/
│   │       └── index.astro      # 中文首页
│   ├── components/
│   │   ├── JsonEditor.astro     # 编辑器组件
│   │   ├── JsonTreeView.astro   # 树形视图组件
│   │   └── Sidebar.astro        # 导航侧边栏
│   ├── layouts/
│   │   └── BaseLayout.astro     # 基础布局
│   └── i18n/
│       └── locales.js           # 翻译文件（英文/中文）
├── public/                      # 静态资源
├── dist/                        # 构建输出
├── astro.config.mjs             # Astro 配置
├── package.json                 # 依赖项
├── README.md                    # 英文文档
├── README.zh.md                 # 中文文档（本文件）
└── FEATURES_SUMMARY.md          # 功能总结
```

---

## 🚢 部署

### Cloudflare Pages（推荐）

#### 方式 1：直接上传

```bash
# 构建项目
npm run build

# 使用 Wrangler CLI 部署
npm install -g wrangler
wrangler login
wrangler pages deploy dist
```

#### 方式 2：Git 集成

1. 将代码推送到 GitHub/GitLab
2. 连接仓库到 Cloudflare Pages
3. 配置构建设置：
   - **框架**：Astro
   - **构建命令**：`npm run build`
   - **输出目录**：`dist`
   - **Node.js 版本**：18 或 20
4. 推送时自动部署

### 其他平台

#### Vercel
```bash
npm install -g vercel
vercel
```

#### Netlify
```bash
npm install -g netlify-cli
netlify deploy --prod
```

### 环境变量

无需环境变量！应用完全在浏览器中运行。

---

## 🔑 SEO 关键词

本工具针对以下关键词进行了优化：

### 核心关键词
- json formatter
- json validator
- json parser
- json viewer
- json editor
- json beautifier
- json minify
- json compare
- json diff

### 转换关键词
- json to yaml
- json to xml
- json to csv
- json to excel
- json to html
- json to pdf
- json to table
- json to dart
- json to go
- json to rust
- json to python
- json to c
- json to typescript

### 功能关键词
- json schema
- json escape
- json stringify
- json pretty print
- json format
- json lint
- json viewer online
- json formatter online

### 中文关键词
- json格式化
- json在线解析
- json在线格式化
- json 整形
- json美化
- json转yaml
- json转表格
- json转pdf
- json转excel
- json对比工具
- json编辑器

---

## 🎓 使用场景

### 面向开发者
- ✅ 调试 API 响应
- ✅ 生成 TypeScript 接口
- ✅ 转换配置文件
- ✅ 对比 JSON 差异
- ✅ 生成数据模型

### 面向数据分析师
- ✅ 将 JSON 转换为 CSV/Excel
- ✅ 将数据可视化为表格
- ✅ 提取特定字段
- ✅ 验证数据结构

### 面向测试工程师
- ✅ 对比 API 响应
- ✅ 验证 JSON Schema
- ✅ 格式化错误消息
- ✅ 测试数据转换

### 面向学生/学习者
- ✅ 学习 JSON 结构
- ✅ 理解数据格式
- ✅ 练习数据转换
- ✅ 研究代码生成

---

## 🌟 核心功能总结

| 功能 | 描述 | 状态 |
|------|------|------|
| 智能输入 | 自动检测 15+ 种格式 | ✅ |
| JSON 验证 | 实时错误检查 | ✅ |
| 格式转换 | 10+ 种输出格式 | ✅ |
| 代码生成 | 7 种编程语言 | ✅ |
| JSON 对比 | 可视化差异工具 | ✅ |
| 字段提取 | JavaScript 表达式 | ✅ |
| 表格视图 | HTML 表格生成 | ✅ |
| 历史记录 | 本地存储 | ✅ |
| 移动端支持 | 响应式设计 | ✅ |
| 双语支持 | 英文 + 中文 | ✅ |
| 隐私保护 | 100% 客户端 | ✅ |
| 离线可用 | 无需互联网 | ✅ |

---

## 🤝 贡献

欢迎贡献！请随时提交问题或拉取请求。

### 开发设置

```bash
# Fork 仓库
# 克隆你的 Fork
git clone https://github.com/YOUR-USERNAME/neo-dev-toolkit.git

# 创建功能分支
git checkout -b feature/amazing-feature

# 进行更改
# 提交更改
git commit -m '添加某个功能'

# 推送到分支
git push origin feature/amazing-feature

# 创建 Pull Request
```

---

## 📊 性能指标

- ⚡ **格式化速度**：1MB JSON < 100ms
- 📦 **打包大小**：< 500KB（gzip）
- 🚀 **加载时间**：3G 网络 < 2s
- 💾 **内存使用**：10MB JSON < 100MB
- 🔄 **转换速度**：大多数格式 < 500ms

---

## 🔒 隐私与安全

- ✅ **无服务器上传**：所有处理在浏览器中进行
- ✅ **无分析代码**：无跟踪代码
- ✅ **无 Cookie**：无用户跟踪
- ✅ **HTTPS**：安全连接
- ✅ **开源**：完全可审计的代码
- ✅ **本地存储**：历史记录保留在您的设备上

---

## 📝 许可证

MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 🙏 致谢

- [Astro](https://astro.build) - 优秀的 Web 框架
- [Monaco Editor](https://microsoft.github.io/monaco-editor/) - 强大的代码编辑器
- [Cloudflare](https://cloudflare.com) - 托管与 CDN

---

## 📞 技术支持

- 📧 邮箱：support@tojsons.com
- 🐛 问题反馈：[GitHub Issues](https://github.com/cenyi/neo-dev-toolkit/issues)
- 💬 讨论区：[GitHub Discussions](https://github.com/cenyi/neo-dev-toolkit/discussions)
- 📖 文档：[完整文档](https://tojsons.com/docs)

---

<div align="center">

**由 JSON Tools 团队用 ❤️ 制作**

⭐ 在 GitHub 上给我们星标 — 这对我们很有帮助！

🔗 [在线演示](https://tojsons.com) • 📖 [文档](https://tojsons.com/docs) • 🐛 [问题反馈](https://github.com/cenyi/neo-dev-toolkit/issues)

[English](README.md) | 简体中文
</div>
