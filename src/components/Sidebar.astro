---
const { t = { navHome: "Home", navAbout: "About", navSettings: "Settings", navLanguage: "Language", navPrivacy: "Privacy Policy", navTerms: "Terms of Service", navContact: "Contact Us" } } = Astro.props;

// 获取当前页面路径
const currentPath = Astro.url.pathname;
---

<div class="sidebar" id="sidebar">
  <script is:inline>
    // 在页面渲染前立即设置折叠状态，避免展开→折叠的动画
    (function() {
      if (localStorage.getItem('sidebarCollapsed') === 'true') {
        document.getElementById('sidebar').classList.add('collapsed');
      }
    })();
  </script>
  <div class="sidebar-header">
    <h2 class="logo">JSON Formatter</h2>
    <button class="toggle-btn" onclick="toggleSidebar()">
      <span class="hamburger"></span>
      <span class="hamburger"></span>
      <span class="hamburger"></span>
    </button>
  </div>
  <nav class="nav-menu">
    <ul>
      <li><a href="/" class={`nav-link ${currentPath === '/' ? 'active' : ''}`} title={t.navHome}>
        <span class="nav-icon">🏠</span>
        <span class="nav-text">{t.navHome}</span>
      </a></li>
      <li><a href="/about" class={`nav-link ${currentPath === '/about' ? 'active' : ''}`} title={t.navAbout}>
        <span class="nav-icon">ℹ️</span>
        <span class="nav-text">{t.navAbout}</span>
      </a></li>
      <li><a href="/settings" class={`nav-link ${currentPath === '/settings' ? 'active' : ''}`} title={t.navSettings}>
        <span class="nav-icon">⚙️</span>
        <span class="nav-text">{t.navSettings}</span>
      </a></li>
      <li><a href="/privacy" class={`nav-link ${currentPath === '/privacy' ? 'active' : ''}`} title={t.navPrivacy}>
        <span class="nav-icon">🔒</span>
        <span class="nav-text">{t.navPrivacy}</span>
      </a></li>
      <li><a href="/terms" class={`nav-link ${currentPath === '/terms' ? 'active' : ''}`} title={t.navTerms}>
        <span class="nav-icon">📜</span>
        <span class="nav-text">{t.navTerms}</span>
      </a></li>
      <li><a href="/contact" class={`nav-link ${currentPath === '/contact' ? 'active' : ''}`} title={t.navContact}>
        <span class="nav-icon">📧</span>
        <span class="nav-text">{t.navContact}</span>
      </a></li>
      <li><a href="#" class="nav-link" onclick="event.preventDefault(); switchLanguage();" title={t.navLanguage}>
        <span class="nav-icon">🌐</span>
        <span class="nav-text">{t.navLanguage}</span>
      </a></li>
    </ul>
  </nav>
</div>

<style>
  .sidebar {
    width: var(--sidebar-width);
    background: linear-gradient(180deg, #f8f9fa 0%, #f1f3f5 100%);
    height: 100%;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 100;
    box-shadow: 2px 0 8px rgba(0,0,0,0.08);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    border-right: 1px solid #e9ecef;
  }

  .sidebar.collapsed {
    width: var(--sidebar-collapsed-width);
  }

  .sidebar-header {
    padding: 16px 18px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #dee2e6;
    background: rgba(255, 255, 255, 0.5);
  }

  .sidebar.collapsed .sidebar-header {
    justify-content: center;
    padding: 16px 0;
  }

  .logo {
    font-size: 1.05rem;
    font-weight: 700;
    white-space: nowrap;
    overflow: hidden;
    transition: opacity 0.25s ease, width 0.25s ease;
    color: #1f2937;
    letter-spacing: -0.02em;
  }

  .sidebar.collapsed .logo {
    opacity: 0;
    width: 0;
    height: 0;
    overflow: hidden;
  }

  .toggle-btn {
    background: rgba(255, 255, 255, 0.8);
    border: 1px solid #dee2e6;
    border-radius: 6px;
    cursor: pointer;
    width: 32px;
    height: 32px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    transition: all 0.2s;
  }

  .toggle-btn:hover {
    background: #fff;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
  }

  .hamburger {
    width: 18px;
    height: 2px;
    background-color: var(--text-color);
    margin: 2px 0;
    transition: 0.25s;
    border-radius: 1px;
  }

  .nav-menu {
    padding: 16px 0;
    flex-grow: 1;
  }

  .nav-menu ul {
    list-style: none;
  }

  .nav-menu li {
    margin-bottom: 3px;
  }

  /* 移除旧的规则，使用新的规则只隐藏文字 */

  .nav-link {
    display: block;
    padding: 10px 18px;
    text-decoration: none;
    color: var(--text-color);
    transition: all 0.2s;
    display: flex;
    align-items: center;
    font-size: 0.9rem;
    font-weight: 500;
    border-radius: 0;
  }

  .nav-link:hover {
    background-color: rgba(59, 130, 246, 0.08);
    color: #2563eb;
  }

  .nav-link.active {
    background: linear-gradient(90deg, rgba(59, 130, 246, 0.15) 0%, rgba(59, 130, 246, 0.05) 100%);
    color: #2563eb;
    border-left: 3px solid #2563eb;
    padding-left: 15px;
  }

  /* 导航图标和文字 */
  .nav-icon {
    margin-right: 10px;
    min-width: 18px;
    text-align: center;
    font-size: 1rem;
  }

  .nav-text {
    white-space: nowrap;
  }

  /* 折叠时隐藏文字，只显示图标 */
  .sidebar.collapsed .nav-text {
    display: none;
  }

  .sidebar.collapsed .nav-link {
    justify-content: center;
    padding: 12px 0;
    text-align: center;
  }

  .sidebar.collapsed .nav-icon {
    margin-right: 0;
    display: inline-block;
  }

  /* 确保折叠时导航链接内的所有元素都居中 */
  .sidebar.collapsed .nav-menu li {
    text-align: center;
  }

  .sidebar.collapsed .nav-menu ul {
    padding: 0;
  }
</style>

<script is:global>
  function toggleSidebar() {
    const sidebar = document.getElementById('sidebar');
    const main = document.querySelector('main');
    sidebar.classList.toggle('collapsed');

    // 更新主内容区域的 margin-left
    if (sidebar.classList.contains('collapsed')) {
      main.style.marginLeft = 'var(--sidebar-collapsed-width)';
      // 保存折叠状态到 localStorage
      localStorage.setItem('sidebarCollapsed', 'true');
    } else {
      main.style.marginLeft = 'var(--sidebar-width)';
      // 保存展开状态到 localStorage
      localStorage.setItem('sidebarCollapsed', 'false');
    }

    // 更新全局状态
    window.appState.sidebarCollapsed = sidebar.classList.contains('collapsed');
  }

  function switchLanguage() {
    // 切换语言
    const currentLang = window.appState.lang;
    const newLang = currentLang === 'en' ? 'zh' : 'en';
    window.appState.lang = newLang;

    // 更新URL参数
    const url = new URL(window.location);
    
    if (newLang === 'en') {
      // 默认语言，移除lang参数
      url.searchParams.delete('lang');
    } else {
      // 非默认语言，设置lang参数
      url.searchParams.set('lang', newLang);
    }
    
    window.location.href = url.toString();
  }

  // 初始化时设置主内容区域的 margin-left（与侧边栏状态保持一致）
  window.addEventListener('DOMContentLoaded', () => {
    const sidebar = document.getElementById('sidebar');
    const main = document.querySelector('main');

    // 根据侧边栏的当前状态设置 main 的 margin-left
    if (sidebar.classList.contains('collapsed')) {
      main.style.marginLeft = 'var(--sidebar-collapsed-width)';
      window.appState.sidebarCollapsed = true;
    } else {
      main.style.marginLeft = 'var(--sidebar-width)';
      window.appState.sidebarCollapsed = false;
    }
  });
</script>