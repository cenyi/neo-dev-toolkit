---
import BaseLayout from '../layouts/BaseLayout.astro';
import Sidebar from '../components/Sidebar.astro';
import { locales } from '../i18n/locales.js';

// 英文关于页面，固定使用英文语言
const lang = 'en-US';
const t = locales['en'];
---

<BaseLayout title={`About - ${t.title}`} lang={lang}>
  <Sidebar slot="sidebar" t={t} />

  <div class="about-container">
    <h1>About JSON Formatter</h1>
    <p>This is a powerful online JSON formatting and processing tool that supports conversion and processing of multiple data formats.</p>

    <h2>Features</h2>
    <ul>
      <li>JSON formatting and validation</li>
      <li>URL parameters, XML, YAML to JSON conversion</li>
      <li>JavaScript function for extracting JSON fields</li>
      <li>JSON compression, escaping, and conversion to other formats</li>
      <li>Multi-language support (English and Chinese)</li>
    </ul>

    <h2>Tech Stack</h2>
    <p>This project is built with the Astro framework, providing high performance and a modern frontend experience.</p>
  </div>
</BaseLayout>

<style>
  .about-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
  }
  
  .about-container h1 {
    color: var(--text-color);
    margin-bottom: 20px;
  }
  
  .about-container h2 {
    color: var(--text-color);
    margin: 20px 0 10px;
  }
  
  .about-container p {
    margin-bottom: 15px;
    line-height: 1.6;
  }
  
  .about-container ul {
    margin-left: 20px;
    margin-bottom: 20px;
  }
  
  .about-container li {
    margin-bottom: 8px;
    line-height: 1.6;
  }
</style>