---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
import { locales } from '../../i18n/locales.js';

// 获取语言参数
const url = new URL(Astro.request.url);
const lang = 'zh';
const t = locales[lang] || locales.en;
---

<BaseLayout title={`关于 - ${t.title}`} lang={lang}>
  <Sidebar slot="sidebar" t={t} />
  
  <div class="about-container">
    <h1>关于 JSON 格式化工具</h1>
    <p>这是一个功能强大的在线JSON格式化和处理工具，支持多种数据格式的转换和处理。</p>
    
    <h2>功能特性</h2>
    <ul>
      <li>JSON格式化与验证</li>
      <li>URL参数、XML、YAML转JSON</li>
      <li>JavaScript函数提取JSON字段</li>
      <li>JSON压缩、转义、转换为其他格式</li>
      <li>多语言支持（中英文）</li>
    </ul>
    
    <h2>技术栈</h2>
    <p>该项目使用Astro框架构建，具有高性能和现代化的前端体验。</p>
  </div>
</BaseLayout>

<style>
  .about-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
  }
  
  .about-container h1 {
    color: var(--text-color);
    margin-bottom: 20px;
  }
  
  .about-container h2 {
    color: var(--text-color);
    margin: 20px 0 10px;
  }
  
  .about-container p {
    margin-bottom: 15px;
    line-height: 1.6;
  }
  
  .about-container ul {
    margin-left: 20px;
    margin-bottom: 20px;
  }
  
  .about-container li {
    margin-bottom: 8px;
    line-height: 1.6;
  }
</style>