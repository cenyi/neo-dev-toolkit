---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
import LanguageSwitcher from '../../components/LanguageSwitcher.astro';
import zhTranslations from '../../i18n/locales/zh.js';

const lang = 'zh-CN';
const t = zhTranslations;
---

<BaseLayout
  title="JSON压缩工具 - 在线压缩JSON减小文件大小"
  lang={lang}
  description="免费的在线JSON压缩工具。压缩JSON移除空格和缩进，减小文件大小。显示压缩前后大小和节省比例。"
  keywords="JSON压缩, JSON minify, 压缩JSON, json compressor"
>
  <div slot="header">
    <LanguageSwitcher currentLocale="zh" />
  </div>
  <Sidebar slot="sidebar" t={t} />

  <div class="json-formatter-container">
    <div class="editor-workspace">
      <div class="input-area">
        <div class="input-section">
          <h1 class="section-label">JSON压缩工具 - 压缩JSON减小文件大小</h1>
          <div class="input-editor-wrapper">
            <div id="monaco-editor-container" class="monaco-editor-container"></div>
          </div>
        </div>

        <div id="validation-error" class="validation-error"></div>

        <div class="output-controls">
          <button id="minify-btn">压缩JSON</button>
          <button id="format-btn">格式化JSON</button>
          <button id="copy-btn">复制结果</button>
        </div>

        <div class="stats-section" id="stats-section">
          <div class="stat-item">
            <span class="stat-label">原始大小:</span>
            <span class="stat-value" id="original-size">0 bytes</span>
          </div>
          <div class="stat-item">
            <span class="stat-label">压缩后:</span>
            <span class="stat-value" id="minified-size">0 bytes</span>
          </div>
          <div class="stat-item">
            <span class="stat-label">节省:</span>
            <span class="stat-value" id="savings">0%</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="content-section">
    <h2>JSON压缩工具 - 在线压缩JSON</h2>
    <p>欢迎使用我们强大的JSON压缩工具，帮助开发者通过移除不必要的空格、换行和缩进来压缩JSON数据。这种优化可显著减小文件大小，非常适合生产部署、API响应和数据存储场景。</p>

    <p>压缩JSON对于优化Web应用程序性能至关重要。当通过网络传输JSON时，每个字节都很重要。我们的JSON压缩工具在保留数据结构的同时移除所有不必要的字符，从而产生更小的有效载荷，加载更快，占用更少带宽。</p>

    <h3>为什么压缩JSON？</h3>
    <ul>
      <li><strong>减少带宽：</strong>更小的有效载荷意味着更快的数据传输</li>
      <li><strong>加载更快：</strong>要解析的数据更少意味着渲染更快</li>
      <li><strong>节省成本：</strong>降低带宽使用减少基础设施成本</li>
      <li><strong>提高性能：</strong>更小的响应提高整体应用程序速度</li>
      <li><strong>API优化：</strong>许多API默认返回压缩的JSON</li>
    </ul>

    <h3>如何压缩JSON</h3>
    <ol>
      <li><strong>粘贴JSON：</strong>在编辑器中输入或粘贴格式化的JSON</li>
      <li><strong>查看统计：</strong>查看原始大小、压缩后大小和节省百分比</li>
      <li><strong>点击压缩：</strong>工具移除所有不必要的空格</li>
      <li><strong>复制结果：</strong>将压缩的JSON复制到剪贴板</li>
    </ol>

    <h3>压缩效果统计</h3>
    <p>节省的空间取决于JSON结构。以下是典型示例：</p>
    <ul>
      <li><strong>简单对象：</strong>减少20-30%</li>
      <li><strong>嵌套对象：</strong>减少30-40%</li>
      <li><strong>大型数组：</strong>减少40-50%</li>
      <li><strong>深层嵌套数据：</strong>减少高达60%</li>
    </ul>
    <p>格式化的JSON中的空格和缩进越多，节省的空间就越多。</p>

    <h3>使用场景</h3>
    <ul>
      <li><strong>生产API：</strong>向客户端提供更小的响应</li>
      <li><strong>数据库存储：</strong>减少存储需求</li>
      <li><strong>缓存：</strong>用更小的有效载荷存储更多缓存项</li>
      <li><strong>Webhook：</strong>优化传出的webhook有效载荷</li>
      <li><strong>移动应用：</strong>减少移动用户的数据使用</li>
      <li><strong>实时应用：</strong>用更小的消息实现更快的更新</li>
    </ul>

    <h3>相关工具</h3>
    <ul>
      <li><a href="/zh/formatter">JSON格式化工具</a> - 格式化JSON提高可读性</li>
      <li><a href="/zh/validator">JSON校验工具</a> - 校验JSON语法</li>
      <li><a href="/zh/compare">JSON对比工具</a> - 对比两个JSON文件</li>
      <li><a href="/zh/converter">JSON转换工具</a> - 转换为其他格式</li>
    </ul>

    <h3>SEO关键词</h3>
    <p>JSON压缩, JSON minify, 压缩JSON, json compressor, json优化, 减小JSON大小, 在线JSON压缩, 免费JSON压缩.</p>

    <h3>开始压缩</h3>
    <p>立即尝试我们的免费JSON压缩工具，访问 tojsons.com/minify，瞬间减小JSON文件大小！</p>
  </div>

  <script type="module">
    window.MonacoEnvironment = {
      getWorkerUrl: function (workerId, label) {
        return `https://cdn.jsdelivr.net/npm/monaco-editor@0.45.0/min/vs/base/workers/${label === 'json' ? 'json.worker' : 'editor.worker'}.js`;
      }
    };

    import * as monaco from 'https://esm.sh/monaco-editor@0.45.0';

    let editor = null;

    document.addEventListener('DOMContentLoaded', () => {
      initializeMonacoEditor();
      setupEventListeners();
    });

    function initializeMonacoEditor() {
      const container = document.getElementById('monaco-editor-container');
      if (!container) return;

      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          if (container.offsetWidth === 0 || container.offsetHeight === 0) {
            setTimeout(() => createEditor(container), 50);
          } else {
            createEditor(container);
          }
        });
      });
    }

    function createEditor(container) {
      editor = monaco.editor.create(container, {
        value: '',
        language: 'json',
        theme: 'vs',
        automaticLayout: true,
        fontSize: 13,
        fontFamily: "'Monaco', 'Menlo', 'Ubuntu Mono', monospace",
        minimap: { enabled: false },
        lineNumbers: 'on',
        scrollBeyondLastLine: false,
        wordWrap: 'off',
        tabSize: 2,
        padding: { top: 12, bottom: 12 }
      });

      editor.onDidChangeModelContent(() => {
        updateStats();
        validateJson();
      });
    }

    function updateStats() {
      if (!editor) return;
      const original = editor.getValue();
      const originalSize = new Blob([original]).size;
      
      try {
        const parsed = JSON.parse(original);
        const minified = JSON.stringify(parsed);
        const minifiedSize = new Blob([minified]).size;
        const savings = originalSize > 0 ? ((originalSize - minifiedSize) / originalSize * 100).toFixed(1) : 0;

        document.getElementById('original-size').textContent = formatBytes(originalSize);
        document.getElementById('minified-size').textContent = formatBytes(minifiedSize);
        document.getElementById('savings').textContent = savings + '%';
      } catch (e) {
        document.getElementById('original-size').textContent = formatBytes(originalSize);
        document.getElementById('minified-size').textContent = '-';
        document.getElementById('savings').textContent = '-';
      }
    }

    function formatBytes(bytes) {
      if (bytes === 0) return '0 bytes';
      const k = 1024;
      const sizes = ['bytes', 'KB', 'MB'];
      const i = Math.floor(Math.log(bytes) / Math.log(k));
      return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
    }

    function validateJson() {
      const errorElement = document.getElementById('validation-error');
      if (!editor || !errorElement) return;

      try {
        JSON.parse(editor.getValue());
        errorElement.className = 'validation-error valid';
        errorElement.textContent = '✓ 有效的JSON';
      } catch (e) {
        errorElement.className = 'validation-error invalid';
        errorElement.textContent = '✗ ' + e.message;
      }
    }

    function setupEventListeners() {
      document.getElementById('minify-btn').addEventListener('click', () => {
        if (!editor) return;
        try {
          const parsed = JSON.parse(editor.getValue());
          editor.setValue(JSON.stringify(parsed));
          showToast('JSON已压缩！');
        } catch (e) {
          showToast('无效JSON: ' + e.message);
        }
      });

      document.getElementById('format-btn').addEventListener('click', () => {
        if (!editor) return;
        try {
          const parsed = JSON.parse(editor.getValue());
          editor.setValue(JSON.stringify(parsed, null, 2));
          showToast('JSON已格式化！');
        } catch (e) {
          showToast('无效JSON: ' + e.message);
        }
      });

      document.getElementById('copy-btn').addEventListener('click', async () => {
        if (!editor) return;
        try {
          await navigator.clipboard.writeText(editor.getValue());
          showToast('已复制到剪贴板！');
        } catch (e) {
          showToast('复制失败');
        }
      });
    }

    function showToast(message) {
      const existing = document.querySelector('.toast-message');
      if (existing) existing.remove();
      const toast = document.createElement('div');
      toast.className = 'toast-message';
      toast.textContent = message;
      toast.style.cssText = 'position:fixed;bottom:20px;right:20px;background:#22c55e;color:white;padding:12px 20px;border-radius:6px;z-index:9999';
      document.body.appendChild(toast);
      setTimeout(() => toast.remove(), 3000);
    }
  </script>

  <style>
    .json-formatter-container { max-width: 1800px; margin: 0 auto; padding: 8px; height: calc(100vh - var(--sidebar-height, 40px)); display: flex; flex-direction: column; }
    .editor-workspace { flex: 1; min-height: 0; }
    .input-area { flex: 1; display: flex; flex-direction: column; gap: 10px; }
    .input-section { flex: 1; display: flex; flex-direction: column; min-height: 0; }
    h1.section-label { font-size: 1rem; font-weight: 700; color: #0f172a; margin-bottom: 8px; }
    .input-editor-wrapper { display: flex; border: 1px solid #e1e4e8; border-radius: 8px; overflow: hidden; flex: 1; min-height: 0; }
    .monaco-editor-container { flex: 1; width: 100%; min-height: 300px; }

    .validation-error { padding: 12px 16px; border-radius: 6px; font-size: 0.875rem; margin-bottom: 10px; }
    .validation-error.valid { background: #dcfce7; border: 1px solid #86efac; color: #166534; }
    .validation-error.invalid { background: #fee2e2; border: 1px solid #fca5a5; color: #991b1b; }

    .output-controls { display: flex; gap: 6px; }
    .output-controls button { padding: 8px 16px; background: #3b82f6; color: white; border: none; border-radius: 5px; cursor: pointer; }
    .output-controls button:hover { background: #2563eb; }

    .stats-section { display: flex; gap: 20px; padding: 12px; background: #f8fafc; border-radius: 6px; }
    .stat-item { display: flex; gap: 8px; }
    .stat-label { color: #64748b; }
    .stat-value { font-weight: 600; color: #0f172a; }

    .content-section { max-width: 900px; margin: 40px auto; padding: 0 20px; }
    .content-section h2 { font-size: 1.75rem; color: #0f172a; margin: 30px 0 15px; }
    .content-section h3 { font-size: 1.25rem; color: #1f2937; margin: 25px 0 12px; }
    .content-section p { color: #4b5563; line-height: 1.7; margin-bottom: 15px; }
    .content-section ul, .content-section ol { color: #4b5563; line-height: 1.8; margin: 10px 0 15px 25px; }
    .content-section li { margin-bottom: 6px; }
    .content-section a { color: #2563eb; text-decoration: none; }
  </style>
</BaseLayout>
